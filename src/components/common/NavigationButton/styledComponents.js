import tw from 'tailwind.macro'
import styled from '@emotion/styled'


export const NavContainer = styled.div`${tw`flex flex-col text-xl justify-center items-center rounded-full `}
                            border:1px solid black;
                            width:20px;`