import tw from 'tailwind.macro'
import styled from '@emotion/styled'

import { Colors } from "../../../../themes/Colors.js"

export const Span = styled.span`${tw``}
                    padding-right:30px;`
                    
                    
export const Table = styled.div`${tw``}`
export const Container = styled.span`${tw`my-4`}`


export const RedText = styled.span`${tw``}
                        color:${Colors.neonRed}`

export const Note = styled.div`${tw`flex my-10`} `

export const BlackText = styled.span`${tw``}
                        color:${Colors.darkBlueGrey}`