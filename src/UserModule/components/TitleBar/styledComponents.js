import tw from 'tailwind.macro'
import styled from '@emotion/styled'

export const Container = styled.div`
   ${tw`flex justify-between py-6 mx-16`}
`

export const Text = styled.div`
   ${tw`text-xl font-semibold text-blue-700`}
`
